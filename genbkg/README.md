# Background
The `generator.py` script in this repository can generate a background toy sample using
```
python generate.py --n {number of data points to be generated} --output {filename where to store the generated data} --params {a json file with the parameters defining the shape}
```